info:
  name: Start Call
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/chat/conversations/:id/call"
  params:
    - name: id
      value: ""
      type: path
      description: Conversation ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Call started
    request:
      url: "{{baseUrl}}/chat/conversations/:id/call"
      method: POST
      params:
        - name: id
          value: ""
          type: path
          description: Conversation ID
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {
              "id": "",
              "conversationId": "",
              "callerId": "",
              "status": "",
              "startedAt": "",
              "endedAt": ""
            }
          }
