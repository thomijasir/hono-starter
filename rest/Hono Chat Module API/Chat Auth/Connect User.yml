info:
  name: Connect User
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/chat/connect"
  body:
    type: json
    data: |-
      {
        "appId": "bnw",
        "userId": "thomijasir",
        "name": "Thomi Jasir",
        "deviceToken": "ffds",
        "deviceType": "android"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
