info:
  name: Create New App Client
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/app-id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
