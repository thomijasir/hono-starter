info:
  name: Add Participants
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/chat/groups/:id/participants"
  params:
    - name: id
      value: ""
      type: path
      description: Group Conversation ID
  body:
    type: json
    data: |-
      {
        "user_ids": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Participants added
    request:
      url: "{{baseUrl}}/chat/groups/:id/participants"
      method: POST
      params:
        - name: id
          value: ""
          type: path
          description: Group Conversation ID
      body:
        type: json
        data: |-
          {
            "user_ids": []
          }
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {
              "success": false
            },
            "message": ""
          }
